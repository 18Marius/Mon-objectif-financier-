<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <title>Mon Objectif Finance</title>
  <style>
    :root{--bg:#0A0A0F;--bg2:#12121A;--card:#1E1E2A;--text:#EAEAF2;--muted:#A1A1AA;--accent:#2BD0D0;--accent2:#8A65E4;--border:rgba(255,255,255,.10)}
    *{box-sizing:border-box;-webkit-tap-highlight-color:transparent}
    html,body{margin:0;padding:0;background:linear-gradient(180deg,var(--bg),var(--bg2));color:var(--text);font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial,sans-serif}
    .card{background:linear-gradient(180deg, rgba(255,255,255,.05), rgba(255,255,255,.02));border:1px solid var(--border);border-radius:14px;padding:14px;box-shadow:0 2px 14px rgba(0,0,0,.25)}
    .btn{display:inline-block;padding:12px 14px;border-radius:12px;background:linear-gradient(135deg,var(--accent),var(--accent2));color:#0B0B10;font-weight:700;border:none;cursor:pointer}
    .muted{color:var(--muted)}
    .bubble{max-width:84%; padding:10px 12px; border-radius:12px; font-size:14px; line-height:1.4}
    .me{align-self:flex-end;background:rgba(43,208,208,.15);border:1px solid rgba(43,208,208,.35)}
    .ai{align-self:flex-start;background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.14); white-space:pre-wrap; word-break:break-word; overflow-wrap:anywhere}
  </style>
</head>
<body>
  <header style="position:sticky;top:0;padding:12px 16px;background:rgba(10,10,15,.92);border-bottom:1px solid rgba(255,255,255,.06);backdrop-filter:blur(10px) saturate(130%);display:flex;justify-content:space-between;align-items:center">
    <div style="display:flex; align-items:center; gap:10px; font-weight:700">
      <div style="width:22px;height:22px;border-radius:6px;background:linear-gradient(135deg,var(--accent),var(--accent2))"></div>
      Mon Objectif Finance
    </div>
    <a href="/desktop.html" class="muted" style="font-size:12px">Version PC</a>
  </header>

  <main style="padding:16px; padding-bottom:96px">
    <div class="card" style="margin-bottom:12px">
      <h2 style="margin:0 0 8px">Mon Coach IA</h2>
      <div id="chat" style="display:flex;flex-direction:column;gap:8px;max-height:52vh;overflow:auto">
        <div class="bubble ai">Salut 👋 Donne-moi ton objectif financier et je te fais un plan détaillé (format ChatGPT structuré).</div>
      </div>
      <div style="display:flex;gap:8px;margin-top:10px">
        <input id="msg" type="text" placeholder="Ex : Épargner 1000 € en 3 mois" style="flex:1;padding:12px;border-radius:12px;background:#0E0E15;color:#fff;border:1px solid rgba(255,255,255,.12)">
        <button id="send" class="btn">Envoyer</button>
      </div>
      <p class="muted" style="margin-top:6px">Réponses longues, en Markdown : Résumé, Analyse, Plan, Exemples, Risques, Actions.</p>
    </div>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
  <script src="/public/ai.js"></script>
</body>
</html>
